import styled from 'styled-components';

export const StyledNavbarLink = styled.li`
    padding: 20px;
    margin: 20px;
    transition: transform .3s;

    &:hover {
        transform: scale(1.4);
    }
`